# Development tooling for CI (Chunk 1 — CI lockdown).
# Installed in addition to requirements.txt. Not shipped to production.
# Pinned with bounded ranges matching the project convention.

# Lint + format (replaces flake8, isort, pyupgrade)
ruff>=0.7.0,<1.0

# Static type check
mypy>=1.13.0,<2.0

# Security linter (OWASP-style)
bandit>=1.7.10,<2.0

# Dependency vulnerability scanner
pip-audit>=2.7.0,<3.0

# Coverage plugin for pytest
pytest-cov>=6.0.0,<7.0

# Async test support
pytest-asyncio>=0.24.0,<1.0
